<UserControl
    x:Class="AioStudy.UI.Views.Overviews.ModuleStatisticsOverviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:MaIc="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
    xmlns:charts="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:AioStudy.UI.Views.Overviews"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:overview="clr-namespace:AioStudy.UI.ViewModels.Overview"
    d:DataContext="{d:DesignInstance Type=overview:ModuleStatisticsOverviewViewmodel}"
    d:DesignHeight="750"
    d:DesignWidth="1200"
    Background="#212226"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="30,0,30,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <DockPanel LastChildFill="False">
                    <Button
                        x:Name="BackButton"
                        Margin="0,0,0,15"
                        Padding="8,6"
                        HorizontalAlignment="Left"
                        Command="{Binding BackCommand}"
                        Style="{StaticResource GrayGradientButton}"
                        ToolTip="Back to Modules">
                        <StackPanel Orientation="Horizontal">
                            <MaIc:MaterialIcon
                                Width="16"
                                Height="16"
                                Margin="0,0,6,0"
                                Kind="ArrowLeft" />
                            <TextBlock FontSize="12" Text="Back to Overview" />
                        </StackPanel>
                    </Button>
                </DockPanel>

                <DockPanel LastChildFill="False">
                    <TextBlock
                        Margin="0,0,0,8"
                        FontSize="36"
                        FontWeight="Bold"
                        Foreground="White">
                        <Run Text="{Binding ModuleName}" />
                        <Run Text=" Statistics" />
                    </TextBlock>

                    <!--  Semester Filter & Actions  -->
                    <StackPanel
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        DockPanel.Dock="Right"
                        Orientation="Horizontal">

                        <Button Command="{Binding PrevDayCommand}" Style="{StaticResource NoHoverButtonStyle}">
                            <MaIc:MaterialIcon
                                Width="27"
                                Height="27"
                                Margin="0,1,0,0"
                                Foreground="White"
                                Kind="ChevronLeft " />
                        </Button>

                        <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                            <ToggleButton
                                x:Name="CalendarToggle"
                                Width="100"
                                Height="37"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                Style="{StaticResource NoHoverToggleButtonStyle}"
                                ToolTip="Open calendar">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="18"
                                    Foreground="White"
                                    Text="{Binding SelectedDate, StringFormat='{}{0:dd.MM.yyyy}', TargetNullValue='-', FallbackValue='20.03.2005'}" />
                            </ToggleButton>

                            <Popup
                                AllowsTransparency="True"
                                IsOpen="{Binding IsChecked, ElementName=CalendarToggle}"
                                Placement="Bottom"
                                PlacementTarget="{Binding ElementName=CalendarToggle}"
                                StaysOpen="False">
                                <Border
                                    Padding="6"
                                    Background="#2B2E31"
                                    BorderBrush="#3A3D45"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    SnapsToDevicePixels="True">
                                    <Calendar
                                        x:Name="InlineCalendar"
                                        Background="white"
                                        BorderThickness="0"
                                        Foreground="White"
                                        SelectedDate="{Binding SelectedDate, Mode=TwoWay, Converter={StaticResource DateOnlyConverter}}"
                                        SelectedDatesChanged="Calendar_SelectedDatesChanged"
                                        SelectionMode="SingleDate">
                                        <Calendar.Resources>
                                            <Style TargetType="CalendarDayButton">
                                                <Setter Property="Foreground" Value="White" />
                                            </Style>
                                        </Calendar.Resources>
                                    </Calendar>
                                </Border>
                            </Popup>
                        </StackPanel>

                        <Button Command="{Binding NextDayCommand}" Style="{StaticResource NoHoverButtonStyle}">
                            <MaIc:MaterialIcon
                                Width="27"
                                Height="27"
                                Margin="0,1,20,0"
                                Foreground="White"
                                Kind="ChevronRight" />
                        </Button>

                        <!--  Refresh Button  -->
                        <Button
                            Width="45"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource GradientButton}"
                            ToolTip="Refresh Grades">
                            <MaIc:MaterialIcon
                                Width="22"
                                Height="22"
                                Kind="Refresh" />
                        </Button>
                    </StackPanel>
                </DockPanel>



                <StackPanel Margin="0,0,0,10" Orientation="Horizontal">
                    <MaIc:MaterialIcon
                        Width="16"
                        Height="16"
                        Margin="0,0,6,0"
                        VerticalAlignment="Center"
                        Foreground="#A0AEC0"
                        Kind="ChartLine" />
                    <TextBlock
                        VerticalAlignment="Center"
                        FontSize="14"
                        Foreground="#B0B3BA"
                        Text="See the statistics for this module" />
                </StackPanel>
            </StackPanel>

            <StackPanel
                Grid.Column="1"
                VerticalAlignment="Center"
                Orientation="Horizontal" />
        </Grid>

        <Border
            Grid.Row="1"
            Height="1"
            Margin="30,0,30,0"
            Background="#3A3D45" />

        <Grid Grid.Row="2" Margin="30">
            <Border Margin="30,10,30,30" Padding="10">
                <charts:CartesianChart
                    Grid.Row="2"
                    Background="Transparent"
                    Series="{Binding Series}"
                    XAxes="{Binding XAxes}"
                    YAxes="{Binding YAxes}" />
            </Border>
        </Grid>
    </Grid>
</UserControl>