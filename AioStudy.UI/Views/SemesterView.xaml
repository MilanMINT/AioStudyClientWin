<UserControl
    x:Class="AioStudy.UI.Views.SemesterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:MaIc="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
    xmlns:components="clr-namespace:AioStudy.UI.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:AioStudy.UI.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
    d:DesignHeight="750"
    d:DesignWidth="1200"
    Background="#212226"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!--  Lokale Scrollbar Styles  -->
        <Style x:Key="LocalScrollBarThumb" TargetType="Thumb">
            <Setter Property="Background" Value="#4A4D55" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border
                            Background="#4A4D55"
                            CornerRadius="3"
                            Opacity="0.6" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter Property="Opacity" Value="1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LocalScrollBar" TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Width" Value="6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Width="6" Background="Transparent">
                            <Track Name="PART_Track" IsDirectionReversed="true">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton
                                        Background="Transparent"
                                        Command="ScrollBar.PageUpCommand"
                                        Opacity="0" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource LocalScrollBarThumb}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton
                                        Background="Transparent"
                                        Command="ScrollBar.PageDownCommand"
                                        Opacity="0" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  Moderner Button Style für Header-Buttons  -->

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="1" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <Grid Grid.Row="0" Margin="30,0,30,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Titel Sektion  -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock
                    Margin="0,0,0,4"
                    FontSize="32"
                    FontWeight="Bold"
                    Foreground="White"
                    Text="Semester Overview" />
                <TextBlock
                    FontSize="14"
                    Foreground="#B0B3BA"
                    Text="Manage your semesters" />
            </StackPanel>

            <!--  Button Bereich  -->
            <StackPanel
                Grid.Column="1"
                VerticalAlignment="Center"
                Orientation="Horizontal">

                <xctk:WatermarkTextBox
                    Width="200"
                    Height="40"
                    Margin="0,0,15,0"
                    Padding="3,4,4,0"
                    FontSize="14"
                    Foreground="White"
                    Style="{StaticResource GradientTextBox}"
                    Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    Watermark="Search Semesters..." />

                <!--  Aktualisieren Button  -->
                <Button
                    Width="45"
                    Margin="0,0,15,0"
                    Command="{Binding LoadSemestersCommand}"
                    Foreground="White"
                    Style="{StaticResource GradientButton}"
                    ToolTip="Semester-Liste aktualisieren">
                    <MaIc:MaterialIcon
                        Width="22"
                        Height="22"
                        Kind="Refresh" />
                </Button>

                <!--  Semester hinzufügen Button  -->
                <Button
                    Width="150"
                    Padding="28,8"
                    Command="{Binding AddSemesterCommand}"
                    Foreground="White"
                    Style="{StaticResource GradientButton}"
                    ToolTip="Neues Semester hinzufügen">
                    <TextBlock
                        FontSize="14"
                        FontWeight="Medium"
                        Text="Add Semester" />
                </Button>
            </StackPanel>
        </Grid>

        <!--  Separator  -->
        <Border
            Grid.Row="1"
            Height="1"
            Margin="30,0"
            Background="#3A3D45" />

        <!--  Semester List mit ScrollViewer  -->
        <ScrollViewer
            Grid.Row="2"
            Margin="30,20"
            Padding="0,0,15,0"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <ScrollViewer.Resources>
                <Style BasedOn="{StaticResource LocalScrollBar}" TargetType="ScrollBar" />
            </ScrollViewer.Resources>

            <ItemsControl ItemsSource="{Binding Semesters}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <components:SemesterCard
                            DeleteCommand="{Binding DataContext.DeleteSemesterCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            OpenSemesterOverviewCommand="{Binding DataContext.OpenSemesterOverviewCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Semester="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!--  Empty State  -->
        <Grid Grid.Row="2">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Semesters.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <MaIc:MaterialIcon
                    Width="80"
                    Height="80"
                    Margin="0,0,0,20"
                    Foreground="#4A4D55"
                    Kind="CalendarBlankMultiple" />
                <TextBlock
                    Margin="0,0,0,8"
                    HorizontalAlignment="Center"
                    FontSize="24"
                    FontWeight="Bold"
                    Foreground="#6B7280"
                    Text="No semesters yet" />
                <TextBlock
                    Margin="0,0,0,20"
                    HorizontalAlignment="Center"
                    FontSize="14"
                    Foreground="#8A8D95"
                    Text="Create your first semester to get started" />
                <Button
                    Padding="20,10"
                    Command="{Binding AddSemesterCommand}"
                    Style="{StaticResource GradientButton}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Add First Semester" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
