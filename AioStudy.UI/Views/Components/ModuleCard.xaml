<UserControl x:Class="AioStudy.UI.Views.Components.ModuleCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:MaIc="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:local="clr-namespace:AioStudy.UI.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="450"
             Width="450" Height="250"
             Background="#222324">


    <Border Background="#1F2024"
            x:Name="CardBorder"
            BorderThickness="2"
            CornerRadius="8"
            RenderTransformOrigin="0.5,0.5">

        <Border.Effect>
            <DropShadowEffect x:Name="CardShadow"
                      Color="#000000"
                      Direction="270"
                      ShadowDepth="2"
                      BlurRadius="4"
                      Opacity="0.2"/>
        </Border.Effect>

        <!-- Transform für Scaling hinzufügen -->
        <Border.RenderTransform>
            <ScaleTransform x:Name="CardScale" ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>


        <Border.BorderBrush>
            <LinearGradientBrush>
                <GradientStop Color="{Binding Color, TargetNullValue=#608BC1, FallbackValue=#608BC1}" Offset="0.0"/>
                <GradientStop Color="#133E87" Offset="1.0"/>
                <GradientStop Color="#a5bacc" Offset="0.5"/>
            </LinearGradientBrush>
        </Border.BorderBrush>

        <Border.Triggers>
            <!-- MouseEnter: Vergrößern + Farbe ändern -->
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>

                        <!-- DropShadow Animation -->
                        <DoubleAnimation Storyboard.TargetName="CardShadow"
                                           Storyboard.TargetProperty="ShadowDepth"
                                           To="5" 
                                           Duration="0:0:0.3">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>

                        <DoubleAnimation Storyboard.TargetName="CardShadow"
                                        Storyboard.TargetProperty="BlurRadius"
                                        To="16" 
                                        Duration="0:0:0.3">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>

                        <DoubleAnimation Storyboard.TargetName="CardShadow"
                                       Storyboard.TargetProperty="Opacity"
                                       To="0.5" 
                                       Duration="0:0:0.3">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>

                        <!-- Delete Button einblenden -->
                        <DoubleAnimation Storyboard.TargetName="DeleteButton"
                                       Storyboard.TargetProperty="Opacity"
                                       To="1.0" 
                                       Duration="0:0:0.1"/>

                        <!-- Edit Button einblenden -->
                        <DoubleAnimation Storyboard.TargetName="EditButton"
                                        Storyboard.TargetProperty="Opacity"
                                        To="1.0" 
                                        Duration="0:0:0.1"/>

                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>

            <!-- MouseLeave: Zurück zur normalen Größe + Farbe -->
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>

                        <!-- DropShadow zurück -->
                        <DoubleAnimation Storyboard.TargetName="CardShadow"
                                       Storyboard.TargetProperty="ShadowDepth"
                                       To="2" 
                                       Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>

                        <DoubleAnimation Storyboard.TargetName="CardShadow"
                                        Storyboard.TargetProperty="BlurRadius"
                                        To="4" 
                                        Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>

                        <DoubleAnimation Storyboard.TargetName="CardShadow"
                                           Storyboard.TargetProperty="Opacity"
                                           To="0.2" 
                                           Duration="0:0:0.2">
                            <DoubleAnimation.EasingFunction>
                                <QuadraticEase EasingMode="EaseOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>

                        <!-- Delete Button ausblenden -->
                        <DoubleAnimation Storyboard.TargetName="DeleteButton"
                                           Storyboard.TargetProperty="Opacity"
                                           To="0" 
                                           Duration="0:0:0.2"/>

                        <!-- Edit Button ausblenden -->
                        <DoubleAnimation Storyboard.TargetName="EditButton"
                                        Storyboard.TargetProperty="Opacity"
                                        To="0" 
                                        Duration="0:0:0.1"/>

                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="1"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="23"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>

            <DockPanel LastChildFill="False" Grid.ColumnSpan="2">
                <!-- ECTS Badge -->
                <Border Grid.Column="1"
                        DockPanel.Dock="Right"
                        CornerRadius="12"
                        Padding="7,3"
                        Margin="5,0,15,0"
                        Width="auto"
                        VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#4A5568" Offset="0"/>
                            <GradientStop Color="#2D3748" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Semester.Name, FallbackValue='WiSe25/26'}"
                                    FontSize="15"
                                    FontWeight="SemiBold"
                                    Foreground="#A0AEC0"
                                    TextAlignment="Center"/>
                    </StackPanel>
                </Border>
                <TextBlock Text="{Binding Name, FallbackValue='d32d23 23d 2d23d2 3d2d23d23dd23d2d d2d23d2'}"
                           FontSize="23"
                           FontWeight="Bold"
                           Foreground="#E0E0E0"
                           ToolTip="{Binding Name}"
                           Margin="15 0 0 0"
                           VerticalAlignment="Center"
                           Grid.Row="0" Grid.Column="0"
                           TextTrimming="CharacterEllipsis"/>
            </DockPanel>


            <!-- Separator -->
            <Border Grid.Row="1" Grid.ColumnSpan="2" 
                    Background="#3A3D45" Height="1"
                    Margin="15 0 15 0"/>


            <Grid Grid.Row="2" Grid.ColumnSpan="2" Margin="0,10,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Zeile 1 -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Grid.Row="0"
                            Margin="0,10,0,10">

                    <!-- Examdate -->
                    <Border CornerRadius="10"
                            Padding="12,6"
                            Margin="0 0 10 0"
                            Background="#2C3448"
                            BorderBrush="#3C4F7A"
                            BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Examdate: "
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="#AFC4FF"/>
                            <TextBlock Text="{Binding ExamDateString, FallbackValue='Open'}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="#E5E7EB"/>
                        </StackPanel>
                    </Border>

                    <!-- Studytime -->
                    <Border CornerRadius="10"
                            Padding="12,6"
                            Background="#322C4A"
                            BorderBrush="#5D4E91"
                            BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Studytime: "
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="#D3BFFF"/>
                            <TextBlock Text="{Binding LearnedMinutes, FallbackValue=0, StringFormat=F1, TargetNullValue='N/A'}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="#F4EFFF"/>
                        </StackPanel>
                    </Border>

                </StackPanel>

                <!-- Zeile 2 -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Grid.Row="1"
                            Margin="0,10,0,10">

                    <!-- Credits -->
                    <Border CornerRadius="10"
                            Padding="12,6"
                            Background="#2F3338"
                            BorderBrush="#51565C"
                            Margin="0 0 10 0"
                            BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Credits: "
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="#B0B3B8"/>
                            <TextBlock Text="{Binding ModuleCredits, FallbackValue=6}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="#E0E0E0"/>
                        </StackPanel>
                    </Border>

                    <!-- Status -->
                    <Border CornerRadius="10"
                            Padding="12,6"
                            Background="#3C3522"
                            BorderBrush="#B08F2E"
                            Margin="0 0 10 0"
                            BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Status: "
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="#F6E05E"/>
                            <TextBlock Text="{Binding ExamStatus, FallbackValue='Pending', TargetNullValue='Open'}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="#FFF8C1"/>
                        </StackPanel>
                    </Border>

                    <!-- Grade -->
                    <Border CornerRadius="10"
                            Padding="12,6"
                            Background="#382B4A"
                            BorderBrush="#7E57C2"
                            BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Grade: "
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="#D1B3FF"/>
                            <TextBlock Text="{Binding Grade, FallbackValue=2.3, StringFormat=F1, TargetNullValue='N/A'}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="#F3EFFF"/>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </Grid>




            <StackPanel Orientation="Horizontal" Grid.Row="4" 
                        VerticalAlignment="Center" Margin="9 0 0 0">
                <MaIc:MaterialIcon Kind="CreateOutline" 
                                   Width="12" Height="12" 
                                   VerticalAlignment="Center" 
                                   HorizontalAlignment="Center"
                                   Foreground="#606069"
                                   Margin="0 0 2 0"/>
                <TextBlock Text="{Binding CreatedString, FallbackValue=Test, StringFormat='dd.MM.yyyy'}"
                           Foreground="#606069"
                           Margin="0 0 0 2"
                           TextAlignment="Center"
                           FontSize="11"/>
            </StackPanel>

            <DockPanel Grid.Row="3" Grid.RowSpan="2" LastChildFill="False" Grid.ColumnSpan="2">
                <Button Style="{StaticResource RedGradientButtonThinBorder}"
                        DockPanel.Dock="Right"
                        Grid.Row="3" Grid.Column="1" Grid.RowSpan="2"
                        x:Name="DeleteButton"
                        Command="{Binding DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding Module, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Width="26"
                        Height="26"
                        Margin="10 0 15 0"
                        VerticalContentAlignment="Center"
                        HorizontalContentAlignment="Center"
                        Opacity="1"
                        UseLayoutRounding="True"
                        SnapsToDevicePixels="True">
                    <MaIc:MaterialIcon Kind="DeleteOutline" Width="20" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center"
                                       UseLayoutRounding="True"
                                       Foreground="#8F7979"
                                       SnapsToDevicePixels="True"
                                       Margin="0 0 0 0"/>
                </Button>

                <Button Style="{StaticResource GrayGradientButton}"
                        Grid.Row="3" Grid.Column="0" Grid.RowSpan="2" Grid.ColumnSpan="2"
                        x:Name="EditButton"
                        DockPanel.Dock="Right"
                        Command="{Binding EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding Module, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Width="26"
                        Height="26"
                        VerticalContentAlignment="Center"
                        HorizontalContentAlignment="Center"
                        Opacity="1"
                        UseLayoutRounding="True"
                        SnapsToDevicePixels="True">
                    <MaIc:MaterialIcon Kind="EyeOutline" Width="20" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center"
                                       UseLayoutRounding="True"
                                       Foreground="#787777"
                                       SnapsToDevicePixels="True"
                                       Margin="0 0 0 0"/>
                </Button>
            </DockPanel>

        </Grid>
    </Border>


</UserControl>
