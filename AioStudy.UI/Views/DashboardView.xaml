<UserControl x:Class="AioStudy.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AioStudy.UI.Views" xmlns:viewmodels="clr-namespace:AioStudy.UI.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:DashboardViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Center">
        <StackPanel>
            <TextBlock Text="Dashboard View"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               FontSize="24"
               Foreground="White"/>
            
            <TextBox Width="200" Height="20"
                     Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            </TextBox>
            
            <!-- Debug TextBlock zum Testen -->
            <TextBlock Text="{Binding Text}" 
                       Foreground="Yellow" 
                       Margin="0,10,0,0"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
