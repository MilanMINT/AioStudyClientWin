<UserControl x:Class="AioStudy.UI.Views.ModulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:MaIc="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:local="clr-namespace:AioStudy.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1200"
             Background="#212226">

    <UserControl.Resources>
        <!-- Module Card Style mit Gradient Border -->
        <Style x:Key="ModuleCard" TargetType="Border">
            <Setter Property="Background" Value="#2A2D35"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <LinearGradientBrush>
                        <GradientStop Color="#608BC1" Offset="0.0"/>
                        <GradientStop Color="#133E87" Offset="1.0"/>
                        <GradientStop Color="{Binding }" Offset="0.5"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#4A5568"
                                      Direction="270"
                                      ShadowDepth="4"
                                      BlurRadius="12"
                                      Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#353842" Offset="0.0"/>
                                <GradientStop Color="#2A2D35" Offset="1.0"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="BorderBrush">
                        <Setter.Value>
                            <LinearGradientBrush>
                                <GradientStop Color="#7BA3D3" Offset="0.0"/>
                                <GradientStop Color="#1E4A99" Offset="1.0"/>
                                <GradientStop Color="#B5C7DA" Offset="0.7"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#4A5568"
                                              Direction="270"
                                              ShadowDepth="6"
                                              BlurRadius="16"
                                              Opacity="0.5"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Status Badge Styles -->
        <Style x:Key="StatusBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <!-- Scrollbar Styles -->
        <Style x:Key="LocalScrollBarThumb" TargetType="Thumb">
            <Setter Property="Background" Value="#4A4D55"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#4A4D55" 
                                CornerRadius="3" 
                                Opacity="0.6"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LocalScrollBar" TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent" Width="8">
                            <Track Name="PART_Track" IsDirectionReversed="true">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" 
                                                  Opacity="0" 
                                                  Background="Transparent"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource LocalScrollBarThumb}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" 
                                                  Opacity="0" 
                                                  Background="Transparent"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="1"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="30,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Titel Sektion -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Module Overview"
                           FontSize="32"
                           FontWeight="Bold"
                           Foreground="White"
                           Margin="0,0,0,4"/>
                <TextBlock Text="Manage your study modules and track your progress"
                           FontSize="14"
                           Foreground="#B0B3BA"/>
            </StackPanel>

            <!-- Action Buttons -->
            <StackPanel Grid.Column="1" 
                        Orientation="Horizontal" 
                        VerticalAlignment="Center">

                <!-- Refresh Button -->
                <Button Style="{StaticResource GradientButton}"
                        Background="#353842"
                        Foreground="White"
                        Margin="0,0,15,0"
                        ToolTip="Refresh Module List">
                    <MaIc:MaterialIcon Kind="Refresh"
                                       Width="22"
                                       Height="22"/>
                </Button>

                <!-- Add Module Button -->
                <Button Command="{Binding CreateModuleCommand}"
                        Style="{StaticResource GradientButton}"
                        ToolTip="Add New Module">
                    <StackPanel Orientation="Horizontal">
                        <MaIc:MaterialIcon Kind="Plus"
                                           Width="18"
                                           Height="18"
                                           Margin="0,0,8,0"/>
                        <TextBlock Text="Add Module" 
                                   FontSize="14"
                                   FontWeight="Medium"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Separator -->
        <Border Grid.Row="1" Background="#3A3D45" Height="1" Margin="30,0"/>

        <!-- Module Cards -->
        <ScrollViewer Grid.Row="2" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="15,20" Panel.ZIndex="99999">
            <ScrollViewer.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource LocalScrollBar}"/>
            </ScrollViewer.Resources>

            <ItemsControl ItemsSource="{Binding Modules}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="380" 
                                Height="200"
                                Background="#2A2D35"
                                CornerRadius="12"
                                Margin="15"
                                Padding="20"
                                UseLayoutRounding="True"
                                SnapsToDevicePixels="True"
                                BorderThickness="2"
                                >

                            <!-- Dynamischer BorderBrush basierend auf Module.Color -->
                            <Border.BorderBrush>
                                <LinearGradientBrush>
                                    <GradientStop Color="#608BC1" Offset="0.0"/>
                                    <GradientStop Color="#133E87" Offset="1.0"/>
                                    <!-- Bindet an Module.Color, falls null wird Standard-Hellblau verwendet -->
                                    <GradientStop Color="{Binding Color, TargetNullValue=#a5bacc, FallbackValue=#a5bacc}" Offset="0.5"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>

                            <!-- Shadow Effect -->
                            <Border.Effect>
                                <DropShadowEffect Color="black"
                                      Direction="270"
                                      ShadowDepth="4"
                                      BlurRadius="7"
                                      Opacity="0.6"/>
                            </Border.Effect>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>

                                <!-- Header mit Titel und Actions -->
                                <Grid Grid.Row="0" Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Name}" 
                                                   FontSize="20" 
                                                   FontWeight="Bold" 
                                                   Foreground="White"
                                                   TextTrimming="CharacterEllipsis"
                                                   ToolTip="{Binding Name}"/>
                                    </StackPanel>

                                    <!-- Delete Button -->
                                    <Button Grid.Column="1"
                                            Command="{Binding DataContext.DeleteModuleCommand, RelativeSource={RelativeSource AncestorType=ScrollViewer}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource NoHoverButtonStyle}"
                                            Width="22"
                                            Height="22"
                                            ToolTip="Delete Module">
                                        <Border Background="#FF4757"
                                                CornerRadius="16"
                                                Width="22"
                                                Height="22">
                                            <MaIc:MaterialIcon Kind="DeleteOutline"
                                                   Foreground="White"
                                                   Width="16"
                                                   Height="16"/>
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#FF3742"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                    </Button>
                                </Grid>

                                <!-- Module Stats -->
                                <Grid Grid.Row="1" Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Credits -->
                                    <StackPanel Grid.Column="0" HorizontalAlignment="Left">
                                        <TextBlock Text="Credits" 
                                                   FontSize="11" 
                                                   Foreground="#8A8D95" 
                                                   Margin="0,0,0,2"/>
                                        <TextBlock FontSize="16" 
                                                   FontWeight="Bold" 
                                                   Foreground="#4A90E2">
                                            <TextBlock.Text>
                                                <Binding Path="ModuleCredits" 
                                                         StringFormat="{}{0} CP" 
                                                         TargetNullValue="- CP"/>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </StackPanel>

                                    <!-- Grade -->
                                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                        <TextBlock Text="Grade" 
                                                   FontSize="11" 
                                                   Foreground="#8A8D95" 
                                                   Margin="0,0,0,2"/>
                                        <TextBlock FontSize="16" 
                                                   FontWeight="Bold"
                                                   Foreground="#51CF66">
                                            <TextBlock.Text>
                                                <Binding Path="Grade" 
                                                         StringFormat="{}{0:F1}" 
                                                         TargetNullValue="-"/>
                                            </TextBlock.Text>
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#51CF66"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Grade}" Value="{x:Null}">
                                                            <Setter Property="Foreground" Value="#8A8D95"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>

                                    <!-- Study Time -->
                                    <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                        <TextBlock Text="Study Time" 
                                                   FontSize="11" 
                                                   Foreground="#8A8D95" 
                                                   Margin="0,0,0,2"/>
                                        <TextBlock FontSize="16" 
                                                   FontWeight="Bold" 
                                                   Foreground="#FF8A00">
                                            <TextBlock.Text>
                                                <Binding Path="LearnedMinutes" 
                                                        StringFormat="{}{0}min"/>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>

                                <!-- Exam Date Info -->
                                <StackPanel Grid.Row="2" 
                                            VerticalAlignment="Center"
                                            Margin="0,10">
                                    <TextBlock Text="Exam Date" 
                                               FontSize="11" 
                                               Foreground="#8A8D95" 
                                               Margin="0,0,0,4"/>

                                    <Border Background="#FF8A00">
                                        <Border.Style>
                                            <Style TargetType="Border" BasedOn="{StaticResource StatusBadge}">
                                                <Setter Property="Background" Value="#FF8A00"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ExamDate}" Value="{x:Null}">
                                                        <Setter Property="Background" Value="#4A4D55"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <StackPanel Orientation="Horizontal">
                                            <MaIc:MaterialIcon Kind="Calendar"
                                                   Width="14"
                                                   Height="14"
                                                   Foreground="White"
                                                   Margin="0,0,6,0"/>
                                            <TextBlock Foreground="White" 
                                                       FontSize="12"
                                                       FontWeight="Medium">
                                                <TextBlock.Text>
                                                    <Binding Path="ExamDate" 
                                                 StringFormat="{}{0:dd.MM.yyyy}" 
                                                 TargetNullValue="Not scheduled"/>
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>

                                <!-- Footer mit Created Date -->
                                <Grid Grid.Row="3" 
                                      Margin="0,15,0,0"
                                      Opacity="0.7">
                                    <TextBlock Text="{Binding CreatedString, StringFormat='Created: {0}'}" 
                                               Foreground="#6B7280" 
                                               FontSize="10"
                                               HorizontalAlignment="Left"
                                               VerticalAlignment="Bottom"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <Grid Grid.Row="2"
              Visibility="Collapsed">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Modules.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                <MaIc:MaterialIcon Kind="BookOpenPageVariant"
                                   Width="80"
                                   Height="80"
                                   Foreground="#4A4D55"
                                   Margin="0,0,0,20"/>
                <TextBlock Text="No modules yet"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="#6B7280"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Create your first module to get started"
                           FontSize="14"
                           Foreground="#8A8D95"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>
                <Button Command="{Binding CreateModuleCommand}"
                        Style="{StaticResource GradientButton}"
                        Padding="20,10">
                    <StackPanel Orientation="Horizontal">
                        <MaIc:MaterialIcon Kind="Plus"
                                           Width="16"
                                           Height="16"
                                           Margin="0,0,6,0"/>
                        <TextBlock Text="Add First Module"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
