<UserControl
    x:Class="AioStudy.UI.Views.ModulesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:MaIc="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
    xmlns:components="clr-namespace:AioStudy.UI.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:AioStudy.UI.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
    d:DesignHeight="750"
    d:DesignWidth="1200"
    Background="#212226"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!--  Module Card Style mit Gradient Border  -->
        <Style x:Key="ModuleCard" TargetType="Border">
            <Setter Property="Background" Value="#2A2D35" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Margin" Value="15" />
            <Setter Property="Padding" Value="20" />
            <Setter Property="UseLayoutRounding" Value="True" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <LinearGradientBrush>
                        <GradientStop Offset="0.0" Color="#608BC1" />
                        <GradientStop Offset="1.0" Color="#133E87" />
                        <GradientStop Offset="0.5" Color="{Binding}" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect
                        BlurRadius="12"
                        Direction="270"
                        Opacity="0.3"
                        ShadowDepth="4"
                        Color="#4A5568" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Offset="0.0" Color="#353842" />
                                <GradientStop Offset="1.0" Color="#2A2D35" />
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="BorderBrush">
                        <Setter.Value>
                            <LinearGradientBrush>
                                <GradientStop Offset="0.0" Color="#7BA3D3" />
                                <GradientStop Offset="1.0" Color="#1E4A99" />
                                <GradientStop Offset="0.7" Color="#B5C7DA" />
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect
                                BlurRadius="16"
                                Direction="270"
                                Opacity="0.5"
                                ShadowDepth="6"
                                Color="#4A5568" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!--  Status Badge Styles  -->
        <Style x:Key="StatusBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Left" />
        </Style>

        <!--  Scrollbar Styles  -->
        <Style x:Key="LocalScrollBarThumb" TargetType="Thumb">
            <Setter Property="Background" Value="#4A4D55" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border
                            Background="#4A4D55"
                            CornerRadius="3"
                            Opacity="0.6" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LocalScrollBar" TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Width" Value="8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Width="8" Background="Transparent">
                            <Track Name="PART_Track" IsDirectionReversed="true">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton
                                        Background="Transparent"
                                        Command="ScrollBar.PageUpCommand"
                                        Opacity="0" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource LocalScrollBarThumb}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton
                                        Background="Transparent"
                                        Command="ScrollBar.PageDownCommand"
                                        Opacity="0" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="1" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <Grid Grid.Row="0" Margin="30,0,30,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Titel Sektion  -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock
                    Margin="0,0,0,4"
                    FontSize="32"
                    FontWeight="Bold"
                    Foreground="White"
                    Text="Module Overview" />
                <TextBlock
                    FontSize="14"
                    Foreground="#B0B3BA"
                    Text="Manage your study modules and track your progress" />
            </StackPanel>

            <!--  Action Buttons  -->
            <StackPanel
                Grid.Column="1"
                VerticalAlignment="Center"
                Orientation="Horizontal">

                <xctk:WatermarkTextBox
                    Width="200"
                    Height="40"
                    Margin="0,0,15,0"
                    Padding="3,4,4,0"
                    FontSize="14"
                    Foreground="White"
                    Style="{StaticResource GradientTextBox}"
                    Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                    Watermark="Search Modules..." />


                <!--  Refresh Button  -->
                <Button
                    Width="45"
                    Margin="0,0,15,0"
                    Command="{Binding RefreshModulesCommand}"
                    Foreground="White"
                    Style="{StaticResource GradientButton}"
                    ToolTip="Refresh Module List">
                    <MaIc:MaterialIcon
                        Width="22"
                        Height="22"
                        Kind="Refresh" />
                </Button>

                <!--  Add Module Button  -->
                <Button
                    Padding="10"
                    Command="{Binding CreateModuleCommand}"
                    Style="{StaticResource GradientButton}"
                    ToolTip="Add New Module">
                    <StackPanel Orientation="Horizontal">
                        <MaIc:MaterialIcon
                            Width="18"
                            Height="18"
                            Margin="0,0,8,0"
                            Kind="Plus" />
                        <TextBlock
                            FontSize="14"
                            FontWeight="Medium"
                            Text="Add Module" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!--  Separator  -->
        <Border
            Grid.Row="1"
            Height="1"
            Margin="30,0"
            Background="#3A3D45" />

        <!--  Module Cards  -->
        <ScrollViewer
            Grid.Row="2"
            Margin="15,20"
            Panel.ZIndex="99999"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <ScrollViewer.Resources>
                <Style BasedOn="{StaticResource LocalScrollBar}" TargetType="ScrollBar" />
            </ScrollViewer.Resources>

            <ItemsControl ItemsSource="{Binding Modules}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <components:ModuleCard
                            Margin="10"
                            DeleteCommand="{Binding DataContext.DeleteModuleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Module="{Binding}"
                            OpenModuleOverviewCommand="{Binding DataContext.OpenModuleOverviewCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!--  Empty State  -->
        <Grid Grid.Row="2" Visibility="Collapsed">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Modules.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <MaIc:MaterialIcon
                    Width="80"
                    Height="80"
                    Margin="0,0,0,20"
                    Foreground="#4A4D55"
                    Kind="BookOpenPageVariant" />
                <TextBlock
                    Margin="0,0,0,8"
                    HorizontalAlignment="Center"
                    FontSize="24"
                    FontWeight="Bold"
                    Foreground="#6B7280"
                    Text="No modules yet" />
                <TextBlock
                    Margin="0,0,0,20"
                    HorizontalAlignment="Center"
                    FontSize="14"
                    Foreground="#8A8D95"
                    Text="Create your first module to get started" />
                <Button
                    Padding="20,10"
                    Command="{Binding CreateModuleCommand}"
                    Style="{StaticResource GradientButton}">
                    <StackPanel Orientation="Horizontal">
                        <MaIc:MaterialIcon
                            Width="16"
                            Height="16"
                            Margin="0,0,6,0"
                            Kind="Plus" />
                        <TextBlock Text="Add First Module" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
