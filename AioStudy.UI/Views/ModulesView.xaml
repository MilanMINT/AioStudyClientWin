<UserControl x:Class="AioStudy.UI.Views.ModulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:MaIc="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:components="clr-namespace:AioStudy.UI.Views.Components"
             xmlns:local="clr-namespace:AioStudy.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1200"
             Background="#212226">

    <UserControl.Resources>
        <!-- Module Card Style mit Gradient Border -->
        <Style x:Key="ModuleCard" TargetType="Border">
            <Setter Property="Background" Value="#2A2D35"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <LinearGradientBrush>
                        <GradientStop Color="#608BC1" Offset="0.0"/>
                        <GradientStop Color="#133E87" Offset="1.0"/>
                        <GradientStop Color="{Binding }" Offset="0.5"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#4A5568"
                                      Direction="270"
                                      ShadowDepth="4"
                                      BlurRadius="12"
                                      Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#353842" Offset="0.0"/>
                                <GradientStop Color="#2A2D35" Offset="1.0"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="BorderBrush">
                        <Setter.Value>
                            <LinearGradientBrush>
                                <GradientStop Color="#7BA3D3" Offset="0.0"/>
                                <GradientStop Color="#1E4A99" Offset="1.0"/>
                                <GradientStop Color="#B5C7DA" Offset="0.7"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#4A5568"
                                              Direction="270"
                                              ShadowDepth="6"
                                              BlurRadius="16"
                                              Opacity="0.5"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Status Badge Styles -->
        <Style x:Key="StatusBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <!-- Scrollbar Styles -->
        <Style x:Key="LocalScrollBarThumb" TargetType="Thumb">
            <Setter Property="Background" Value="#4A4D55"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#4A4D55" 
                                CornerRadius="3" 
                                Opacity="0.6"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LocalScrollBar" TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent" Width="8">
                            <Track Name="PART_Track" IsDirectionReversed="true">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" 
                                                  Opacity="0" 
                                                  Background="Transparent"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource LocalScrollBarThumb}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" 
                                                  Opacity="0" 
                                                  Background="Transparent"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="80"/>
            <RowDefinition Height="1"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="30 0 30 0" LastChildFill="False">
            <Button Height="25" BorderThickness="0" Background="Transparent" Margin="0 0 5 0">
                <MaIc:MaterialIcon Kind="ArrowBack"
                                   Width="24"
                                   Height="24"
                                   Foreground="White"/>
            </Button>
            <Button Height="25" BorderThickness="0" Background="Transparent">
                <MaIc:MaterialIcon Kind="ArrowForward"
                       Width="24"
                       Height="24"
                       Foreground="White"/>
            </Button>
        </DockPanel>

        <!-- Header -->
        <Grid Grid.Row="1" Margin="30 0 30 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Titel Sektion -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Module Overview"
                           FontSize="32"
                           FontWeight="Bold"
                           Foreground="White"
                           Margin="0,0,0,4"/>
                <TextBlock Text="Manage your study modules and track your progress"
                           FontSize="14"
                           Foreground="#B0B3BA"/>
            </StackPanel>

            <!-- Action Buttons -->
            <StackPanel Grid.Column="1" 
                        Orientation="Horizontal" 
                        VerticalAlignment="Center">

                <!-- Refresh Button -->
                <Button Style="{StaticResource GradientButton}"
                        Foreground="White"
                        Margin="0,0,15,0"
                        Width="45"
                        ToolTip="Refresh Module List">
                    <MaIc:MaterialIcon Kind="Refresh"
                                       Width="22"
                                       Height="22"/>
                </Button>

                <!-- Add Module Button -->
                <Button Command="{Binding CreateModuleCommand}"
                        Style="{StaticResource GradientButton}"
                        Padding="10"
                        ToolTip="Add New Module">
                    <StackPanel Orientation="Horizontal">
                        <MaIc:MaterialIcon Kind="Plus"
                                           Width="18"
                                           Height="18"
                                           Margin="0,0,8,0"/>
                        <TextBlock Text="Add Module" 
                                   FontSize="14"
                                   FontWeight="Medium"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Separator -->
        <Border Grid.Row="2" Background="#3A3D45" Height="1" Margin="30,0"/>

        <!-- Module Cards -->
        <ScrollViewer Grid.Row="3" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="15,20" Panel.ZIndex="99999">
            <ScrollViewer.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource LocalScrollBar}"/>
            </ScrollViewer.Resources>

            <ItemsControl ItemsSource="{Binding Modules}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <components:ModuleCard Module="{Binding}"
                                               Margin="10"
                                               DeleteCommand="{Binding DataContext.DeleteModuleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               EditCommand="{Binding DataContext.EditModuleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <Grid Grid.Row="2"
              Visibility="Collapsed">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Modules.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                <MaIc:MaterialIcon Kind="BookOpenPageVariant"
                                   Width="80"
                                   Height="80"
                                   Foreground="#4A4D55"
                                   Margin="0,0,0,20"/>
                <TextBlock Text="No modules yet"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="#6B7280"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Create your first module to get started"
                           FontSize="14"
                           Foreground="#8A8D95"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>
                <Button Command="{Binding CreateModuleCommand}"
                        Style="{StaticResource GradientButton}"
                        Padding="20,10">
                    <StackPanel Orientation="Horizontal">
                        <MaIc:MaterialIcon Kind="Plus"
                                           Width="16"
                                           Height="16"
                                           Margin="0,0,6,0"/>
                        <TextBlock Text="Add First Module"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
